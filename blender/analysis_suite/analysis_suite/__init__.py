"""Formula90s deterministic headless 3D analysis suite (ANL-001).

Pure-Python evidence gathering over GLB geometry and read-only adapters to the
existing vehicle pipeline inspector and OBJ validator. Never mutates source
assets; writes only requested JSON reports and temporary test fixtures.
"""

__version__ = '1.0.0'
