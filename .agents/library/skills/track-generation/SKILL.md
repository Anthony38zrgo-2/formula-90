# Track Generation

SCOPE: circuit geometry; centerline; racing surface; barriers; checkpoints; spawn; track-side placement; reconstruction/generation.

POLICY: GEOMETRY != GAMEPLAY-CONTRACT; runtime-affecting delta => validate real track scene/import.
