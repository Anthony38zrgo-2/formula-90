================================================================================
FORMULA-90  —  HANDOFF INSTRUCTIONS (instrucciones.txt)
================================================================================
Generated: 2026-08-18 (facade era)  |  Repo: D:\Formula90s  |  Engine: Godot 4.7.1
Mode: BUILD  |  Do NOT commit unless the user explicitly asks.

AUTHORITY / CONVENTIONS (from .agents/AGENTS.md & PROJECT_STATE.md)
- Fail Faster, Adapt Faster: cheapest falsification before mutation.
- Change ONE physics family at a time. JSON (f1_94_physics.json) is the single
  runtime authority for F1-94 (§3.1 of PROJECT_STATE). runtime > scene > docs.
- Use subagents for independent analysis; 1 hypothesis per iteration.
- Update PROJECT_STATE.md after completing work.

================================================================================
1. CONTEXT DUMP — CURRENT ARCHITECTURE (facade/orquestador)
================================================================================
Canonical runtime is a SINGLE Rust orchestrator (the "facade"), not three
independent modules:

    Godot (GDScript HUD/telemetry)  <-- getters del nodo F90Core
        [ F90Core ]  (nodo GDExtension C++, native/src/core/f90_core.cpp)
            |  _integrate_forces -> f90_core_step (1 llamada, 1 struct F90CoreFrameOut)
            |  _process -> pump_audio -> 1 push_buffer (batch; no Variant por muestra)
        [ game/core ]  formula90_core.dll  (la FACHADA)
            CoreFacade owns: World (game_sim) + AudioModule (vehicle_audio_engine)
                             + ModuleRegistry (SimModule: clima/IA/...)
            .step = física -> módulos -> audio-input -> CoreFrame atómico
        game/sim (World, snapshot) -> vehicle_physics_engine (solver)
        game/audio/engine (mezclador sample-accurate)

- Crates: game/physics/engine (vehicle_physics_engine, solver), game/sim
  (game_sim, mundo autoritativo), game/audio/engine (vehicle_audio_engine),
  game/core (formula90_core, la fachada) — NUEVO.
- Nodo C++: F90Core (native/src/core/f90_core.{hpp,cpp},
  native/include/formula90s/core/f90_core.h). Un solo LoadLibrary de
  formula90_core.dll, un solo handshake f90_core_abi_version()==2.
- F194RustVehicle (native/src/vehicle/f1_94_rust_vehicle.{hpp,cpp}) es el
  RigidBody3D. Cuando F90Core lo conduce (bridge_controlled_ + core_driver_),
  _integrate_forces delega en F90Core::drive_integrate (fuerzas + Godot integra
  gravedad/colisiones, gravity_scale=1.0). Sin F90Core usa su dll legacy
  (vehicle_physics_engine.dll) como fallback.
- Escenas: f1_94_rust.tscn es solo el cuerpo físico (SIN F90Core ni nodo de
  audio); el orquestador vive en vehicle_test_session.tscn y
  sim_bridge_quick_test.tscn (raíz F90Core). f1_94.tscn = legacy GEVP intacto.
- ABI v2 (F90_CORE_ABI_VERSION=2): f90_core_step lleva `aids_mask: u32` (los 8
  bits) en vez del antiguo pulso `toggle_tc`, y existe f90_core_apply_runtime_config
  (espejo de F90RuntimeConfig). Headers: f90_core.h + f90_core.hpp (EAC default
  EXPECTED_ABI_VERSION=2) + formula90_physics.h (F90RuntimeConfig).
- Audiencia de la fachada: docs/ARCHITECTURE.md (sección fachada + checklist de
  módulos), reports/facade_baseline.md (baseline de rendimiento), reports/ (hay
  informe de auditoría y de snap-oversteer producidos por subagentes).

================================================================================
2. WHAT JUST HAPPENED (validated, don't redo)
================================================================================
A) FACADE CONSTRUIDA (F0-F5): crate game/core (CoreFacade, SimModule trait,
   audio module, ffi.rs f90_core_*), nodo C++ F90Core, escenas migradas,
   build_windows.ps1/build_linux.sh integran game/core. 10+ tests Rust OK.
   Validación: headless sim_bridge_quick_test y vehicle_test_session cargan la
   fachada (`[F90Core] facade ready`, ABI=2) y conducen el vehículo; opcional
   `core_cli --parity-sim` da Snapshot byte-idéntico a game_sim.

B) AUDIO ARREGLADO Y OPERATIVO (confirmado por el usuario):
   - En F90Core::_ready se crean los nodos de audio: ensure_vehicle_bus() +
     create_audio_nodes() (generador 44.1kHz/60ms + AudioStreamPlayer no
     posicional + bus "Vehicle"). pump_audio() en _process hace UN push_buffer
     en lote (se acabó el push_frame por muestra). audio_initialized=true en
     sesión gráfica.
   - Se quitó el F90Core duplicado del prefab f1_94_rust.tscn (había DOS -> dos
     fachadas y dos drivers alternando cada frame: causaba "config de física
     rara"). El orquestador vive SOLO en la escena de sesión.

C) FIX P0 — AYUDAS (ESP/TCS) EN LA FACHADA (2026-08-18, validado):
   - El ABI de la fachada perdía el mask de ayudas (solo pulso toggle_tc) y el
     primer frame generaba un toggle espurio que apagaba TCS y congelaba ESP.
     FIX: f90_core_step ahora recibe aids_mask (8 bits) y CoreFacade::step
     aplica AidsMask::from_bits(aids_mask) CADA frame (sim.aids completo +
     sync AidsState). Se eliminó last_aids_mask_/toggle en F90Core.
   - f90_core_apply_runtime_config añadido y enrutado: los setters del vehículo
     (diff_preload, bias, aero, ...) ahora escriben en el core de la fachada en
     modo bridge (F194RustVehicle::apply_runtime_config -> core_driver_ ->
     F90Core::apply_runtime_config -> ffi). Reutiliza
     vehicle_physics_engine::apply_runtime_config_to_sim (extracto público).
   - Tests: facade_applies_full_aids_mask_each_step,
     facade_apply_runtime_config_is_accepted + suite física OK.
   - Ambas DLL (debug y template_release de formula90_core) reconstruidas a ABI v2.

================================================================================
3. AUDITORÍA DEL JSON — HALLAZGOS (ya hechos, NO repetir; úsalos para P1)
================================================================================
Auditoría read-only de game/.../vehicles/f1_94/f1_94_physics.json (subagente).
CONCLUSIÓN: casi todo se usa, pero estas claves se IGNORAN hardcodeadas o muertas:
- surfaces.*.lateral_grip_assist (JSON Road 0.04) -> tire.rs hardcodea 0.05.
- surfaces.*.longitudinal_grip_ratio (JSON Road 0.64) -> tire.rs hardcodea 0.5
  (menos agarre longitudinal -> más patinaje -> sensación "sin TCS"; TC ya OK tras P0).
- differential.slip_transition_threshold_rad_s (JSON 0.90) -> powertrain.rs:505
  hardcodea 0.50.
- input_smoothing_* (JSON on: steering 6.0/throttle 7.5/brake 14.0) ->
  simulation.rs:501,506 hardcodea 20.0/10.0 (filter_inputs).
- automatic_shift (15 claves) y gear_inertia: parseados pero NO usados
  (auto-shift hardcodeado powertrain.rs:137-179; GEVP_GEAR_INERTIA 0.02).
- launch_control_* + bit launch_control, bit auto_clutch, stability_upright_*,
  traction_control_actuator, handbrake_rear_only/abs_interlock/clutch_coupling,
  wheel_hubs, metadatos (vehicle_id/profile_name/units/coordinate_contract):
  no leídas o metadatos.
- brakes.enable_abs solo gobierna la ruta compat; en ruta real el ABS lo manda
  el bit del mask (simulation.rs:225) -> dos fuentes de verdad.
Diferencial de valores vs canónico a decidir con el usuario:
  diff_preload JSON 40 vs canónico 170; contact_patch JSON 0.35 vs 0.21.
Candidatos a "movimientos parásitos": countersteer_assist activo (mask JSON) +
  ESP torque + pitch torque aid + grips hardcodeados + TC off de primer frame ya
  arreglado.

================================================================================
4. NEXT TASK (what the next agent must do)
================================================================================
USER REQUEST (intent): "verificar que todos los parámetros configurables del JSON
  del F1-94 se usan de verdad; parece que no hay ESP/TCS (ya arreglado en P0), pero
  puede haber otras cosas interfiriendo en movimientos parásitos."
+ Aprobado (P0) -> AHORA P1/P2.

--- PHASE P1 — FIDELIDAD DEL JSON (parámetros muertos/hardcodeados) ---
Orden sugerido por impacto en el patinaje/snap oversteer:
1. tire.rs L287-293: cablear surface_lateral_grip_assist y
   surface_longitudinal_grip_ratio DESDE sim.config (VehicleConfig ya los
   almacena: surface_lateral_grip_assist, surface_longitudinal_grip_ratio;
   ver vehicle_config.rs L1569-1570). Sustituir los hardcodes 0.05/0.5. Corrección
   grande de "sensación sin TCS" porque el JSON pide 0.64 (más agarre).
   CUIDADO: cambiar 0.5->0.64 cambia la sensación; validar con A/B headless.
2. powertrain.rs:505: usar diff_slip_transition_threshold_rad_s del config en
   solve_salisbury_differential (en vez de 0.50) o eliminar la clave.
3. simulation.rs: filter_inputs: usar input_smoothing_* (vehicle_config los
   almacena?) o eliminar las claves del JSON si no se quieren.
4. Decidir (con usuario) diff_preload / contact_patch (JSON vs canónico) y
   automatic_shift / gear_inertia (implementar o borrar del schema).
5. P1 adicional de coherencia: brakes.enable_abs vs bit del mask (una fuente);
   enrutar driving_aids.gd (toggles ESTAB/FRENOS/GRIP hoy no-op por
   vehicle_tunable_contract.gd UNSUPPORTED_F1_94_PROPERTIES L19-26) por
   set_aids_enabled_mask, no por propiedades bloqueadas.
Regla Phase C (PROJECT_STATE §8.3): cambia UNA familia a la vez y valida.

--- PHASE P2 — TELEMETRÍA PARA LA RUTA RUST (para poder validar con datos) ---
6. telemetry_manager.gd SOLO captura clase "Vehicle" (GEVP) -> la ruta Rust
   (F194RustVehicle) NO produce CSV. Añadir soporte para registrar el vehículo
   Rust (o exponer la telemetría del core en la facada). Sin esto no hay CSV
   de la sesión que conduces.
7. Cabecera telemetry_manager.gd declara 26 columnas (TC_Active) pero emite 25:
   reparar la columna TC_Active para poder verificar TC desde el CSV.
8. Rebuild de la extensión native RELEASE (libformula90s.windows.template_release)
   tras los cambios P0 (aún stale, sin F90Core). Y rebuild de formula90_core
   template_release cuando se toque la fachada (ABI).

--- VALIDACIÓN (cada fase) ---
- cargo test  (game/physics/engine + game/core al menos) -> EXIT 0.
- scripts/build_windows.ps1 -Configuration debug  -> EXIT 0.
- Headless: .tools/godot/Godot_v4.7.1-stable_win64_console.exe --headless
  --path game --quit-after 240 res://scenes/runtime/sim_bridge_quick_test.tscn
  -> "[F90Core] facade ready (ABI=2)" + "[F90Core->Vehicle]" + sin mismatch.
- Tras cambios en formula90_core: recopiar SIEMPRE debug Y release-named
  (template_release) a game/addons/formula90s/bin (el headless carga primero
  template_release; si queda stale con ABI viejo -> FATAL ABI mismatch).

================================================================================
5. KEY FILES REFERENCE
================================================================================
game/core/                         - la fachada: lib.rs (CoreFacade), ffi.rs
                                     (f90_core_*, ABI v2, layout_tests),
                                     audio.rs, module.rs (SimModule),
                                     modules/{weather,ai}_stub.rs, tests/
game/physics/engine/src/           - solver: vehicle_config.rs (JSON),
                                     simulation.rs (AidsMask, filter_inputs,
                                     filter_inputs L501/506, ESP),
                                     powertrain.rs (TC L310-366, LSD L469-520),
                                     tire.rs (L287-293 grips), ffi.rs
                                     (apply_runtime_config_to_sim)
game/sim/                          - game_sim: world.rs, snapshot.rs, c_abi.rs
native/src/core/f90_core.cpp       - nodo F90Core (drive_integrate, pump_audio,
                                     apply_runtime_config, reset_core_at)
native/include/formula90s/core/f90_core.h / .hpp   - C ABI v2 + F90Core class
native/include/formula90s/vehicle/formula90_physics.h - F90RuntimeConfig
native/src/vehicle/f1_94_rust_vehicle.cpp - bridge_controlled, apply_runtime_config
game/data/vehicles/f1_94/f1_94_physics.json  - fuente de verdad (schema v2)
game/addons/formula90s/scripts/telemetry_manager.gd  - P2 (solo GEVP; 26 vs 25)
game/addons/formula90s/scripts/vehicle_tunable_contract.gd - P1 toggles no-op
game/scenes/runtime/{vehicle_test_session,sim_bridge_quick_test}.tscn - orquestador
scripts/build_windows.ps1 / build_linux.sh - builds (game/core incluido)
docs/ARCHITECTURE.md  reports/facade_baseline.md  PROJECT_STATE.md  (§45 item 11)

================================================================================
6. OPEN DECISIONS FOR NEXT AGENT (ask the user before mutating)
================================================================================
- P1: ¿cablear longitudinal_grip_ratio 0.64 (más agarre) o mantener 0.5? (cambia
  sensación). ¿implementar automatic_shift/gear_inertia o quitarlos del schema?
  ¿reconciliar diff_preload (40 vs 170) y contact_patch (0.35 vs 0.21)?
- P2: ¿dónde registrar la telemetría Rust (extender telemetry_manager vs exponer
  del core)? Aprobarlo antes.
- Do NOT commit unless asked. Update PROJECT_STATE.md (§45) when done.
================================================================================
