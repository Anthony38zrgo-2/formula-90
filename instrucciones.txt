================================================================================
FORMULA-90  —  HANDOFF INSTRUCTIONS (instrucciones.txt)
================================================================================
Generated: 2026-08-21  |  Repo: D:\Formula90s  |  Engine: Godot 4.7.1
Mode: BUILD  |  Do NOT commit unless the user explicitly asks.

AUTHORITY / CONVENTIONS (from .agents/AGENTS.md & PROJECT_STATE.md)
- Fail Faster, Adapt Faster: cheapest falsification before mutation.
- Change ONE physics family at a time. JSON (f1_94_physics.json) is the single
  runtime authority for F1-94 (§3.1 of PROJECT_STATE). runtime > scene > docs.
- Use subagents for independent analysis; 1 hypothesis per iteration.
- Update PROJECT_STATE.md after completing work.

================================================================================
1. CONTEXT DUMP — CURRENT ARCHITECTURE (facade/orquestador)
================================================================================
Canonical runtime is a SINGLE Rust orchestrator (the "facade"), not three
independent modules:

    Godot (GDScript HUD/telemetry)  <-- getters del nodo F90Core
        [ F90Core ]  (nodo GDExtension C++, native/src/core/f90_core.cpp)
            |  _integrate_forces -> f90_core_step (1 llamada, 1 struct F90CoreFrameOut)
            |  _process -> pump_audio -> 1 push_buffer (batch; no Variant por muestra)
            |  drive_integrate -> process_collision_audio -> disparadores de impactos/scrape
        [ game/crates/formula90-core ]  formula90_core.dll  (la FACHADA)
            CoreFacade owns: World (game_sim) + AudioModule (vehicle_audio_engine)
                             + ModuleRegistry (SimModule: clima/IA/...)
            .step = física -> módulos -> audio-input -> CoreFrame atómico
        game/crates/game-sim (World, snapshot) -> vehicle_physics_engine (solver)
        game/crates/vehicle-audio-engine (mezclador sample-accurate en Rust)

- Crates:
  * `game/crates/vehicle-physics-engine` (solver físico determinista)
  * `game/crates/game-sim` (mundo y snapshot)
  * `game/crates/vehicle-audio-engine` (motor de audio sample-accurate, crossfade 5 bandas, camas de suelo y one-shots)
  * `game/crates/formula90-core` (fachada orquestadora unificada)
- Nodo C++: `F90Core` (`native/src/core/f90_core.{hpp,cpp}`). Carga `formula90_core.dll` con ABI v7.
- `F194RustVehicle` (`native/src/vehicle/f1_94_rust_vehicle.{hpp,cpp}`) es el `RigidBody3D`. En `_ready()` tiene activo `set_contact_monitor(true)` y `set_max_contacts_reported(4)`.
- Bank de Audio: `game/sounds/banks/v10_vehicle/` (pipeline híbrido reproducible: samples fuente + síntesis determinista).

================================================================================
2. WHAT WAS JUST IMPLEMENTED AND VALIDATED (2026-08-21)
================================================================================
FASE 1 DE AUDIO — CONEXIÓN DE DISPARADORES HUÉRFANOS Y OVER-RUN BACKFIRE:

A) ENGINE OVER-RUN BACKFIRE (Rust Core):
   - En `game/crates/vehicle-audio-engine/src/mixer.rs`:
     * Agregado `backfire_cooldown_samples: usize` al estado del mezclador.
     * En `VehicleAudioEngine::set_state()`: detecta desaceleración súbita de acelerador
       (`last_throttle >= 0.80 && throttle <= 0.15`) con motor a altas RPM (`rpm >= 12000.0`).
     * Dispara automáticamente `Trigger::Backfire` y selecciona pseudoaleatoriamente
       una sola variante entre `int_backfire.wav` e `int_backfire_2.wav`, manteniendo
       replays deterministas. Aplica cooldown de 350 ms (~15,435 samples a 44.1 kHz).
     * En `VehicleAudioEngine::render()`: decrementa `backfire_cooldown_samples` en cada frame.
     * `engine_start_backfire.wav` conserva el sample de arranque y NO se usa en over-run.
     * Tests cubren umbrales, cooldown real y selección determinista de una sola variante.

B) COLISIONES FÍSICAS Y RASPADO (C++ / Godot RigidBody3D):
   - En `native/src/vehicle/f1_94_rust_vehicle.cpp`:
     * `_ready()` activa `set_contact_monitor(true)` y `set_max_contacts_reported(4)`.
   - En `native/src/core/f90_core.cpp` y `f90_core.hpp`:
     * Implementado `F90Core::process_collision_audio(F194RustVehicle *veh, PhysicsDirectBodyState3D *state, double dt)`:
       - Evalúa `state->get_contact_count()`.
       - Calcula impulso normal y velocidad tangencial contra el plano de colisión.
       - Discrimina grupos del colisionador:
         * `Barrier` / `Wall` / `Armco` / `TireBarrier` / `Guardrail`:
           -> `impact_barrier` si `normal_impact > 3.0f`
           -> `scrape` si `tang_speed > 4.0f` (roce lateral)
         * `Cone` / `Prop` / `DynamicObstacle`: código reservado; fuera de uso actual.
         * Colisiones generales (carrocería / coche a coche):
           -> `impact_hit_1` (impacto suave < 8)
           -> `impact_hit_2` (impacto medio 8-15)
           -> `impact_hit_3` (impacto fuerte 15-25)
           -> `impact_hit_4` (impacto muy fuerte > 25)
           -> `scrape` (roce plano contra suelo con `normal.y > 0.7f`), reproducido con
              `impact_scrape.wav` sintético, corto y de baja intensidad.
       - Detección de fondo plano (*chassis bottoming*): si la compresión de suspensión llega
         al tope ($\ge 75\text{ mm}$) a más de $50\text{ km/h}$, dispara `scrape`.
       - Cooldown de colisión (`collision_cooldown_`) para evitar saturación de audio.

C) BUILD SCRIPT FIX & VALIDACIÓN DE COMPILACIÓN:
   - `scripts/build_windows.ps1` ajustado para evitar que warnings de `cargo` en stderr detengan
     la ejecución por `$ErrorActionPreference='Stop'`.
   - Compilación exitosa de C++ GDExtension (`libformula90s`) y todos los crates de Rust.
   - DLLs actualizadas en `game/addons/formula90s/bin/`:
     * `formula90_core.windows.template_debug.x86_64.dll` (+ `formula90_core.dll`)
     * `vehicle_audio_engine.windows.template_debug.x86_64.dll` (+ `vehicle_audio_engine.dll`)
     * `game_sim.windows.template_debug.x86_64.dll` (+ `game_sim.dll`)
     * `vehicle_physics_engine.windows.template_debug.x86_64.dll`
   - Tests: `cargo test --workspace` (53 tests de física + 39 de audio + 14 de facade) -> 100% PASS.

================================================================================
3. ESTADO DEL BANCO DE AUDIO (v10_vehicle) TRAS FASE 1
================================================================================
Samples en uso activo en tiempo real:
- Motor continuo: `engine_idle.wav`, `engine_low.wav`, `engine_mid.wav`, `engine_high.wav`, `engine_redline.wav`
- Cambios de marcha: `shift_up.wav`, `shift_down.wav`
- Camas de suelo: `surf_grass.wav`, `surf_rumble.wav`, `surf_sand.wav`
- Backfire: `int_backfire.wav` / `int_backfire_2.wav` (una variante pseudoaleatoria por lift-off > 12k RPM).
- Fondo plano: `impact_scrape.wav` (síntesis determinista sutil, 0.22 s, pico 0.32).
- Colisiones / Impactos: `impact_barrier.wav`, `impact_hit_1.wav`, `impact_hit_2.wav`, `impact_hit_3.wav`, `impact_hit_4.wav`.
- Reservado, no usado: `impact_cone.wav`.

- `engine_start_backfire.wav` (sample de backfire durante arranque; no conectar al over-run)
Samples pendientes de siguientes fases:
- `engine_starter.wav` (arranque del coche)
- `engine_limiter.wav` (limitador de pitlane)
- `engine_tc.wav` (modulación acústica de control de tracción)
- `shift_3.wav` (variante alternativa de cambio)
- `impact_fire.wav` (combustión / fuego de escape)
- Ambiente y carrera fuera del banco de vehículo:
  * `Ambient/ambient.mp3` (fondo de circuito)
  * `Ambient/Crowd/fans_national_event_normal.wav` / `fans_national_event_positive.wav` (público)
  * `Ambient/startlight_red.wav` / `Ambient/startlight_greenGO.wav` (semáforo de salida)
  * `GP_toggle.wav` (UI) / `screenshot.wav` (modo foto)

================================================================================
4. PRÓXIMAS FASES RECOMENDADAS
================================================================================
A) FASE 2 DE AUDIO — SECUENCIA DE CARRERA, SEMÁFORO Y AMBIENTE:
   - Conectar sonidos de semáforo `startlight_red.wav` y `startlight_greenGO.wav` en `race_session.gd`.
   - Colocar `AudioStreamPlayer3D` para sonido ambiente `ambient.mp3` y público en tribunas.
   - Conectar sonido de UI `GP_toggle.wav` a la navegación de menús.

B) FASE 3 DE AUDIO — ENRIQUECIMIENTO TÉCNICO:
   - Crear sample / cama de chirrido de neumático sobre asfalto (*tire skid*) activado cuando `slip > 0.15`.
   - Conectar `engine_starter.wav` a la secuencia de encendido.
   - Conectar `engine_limiter.wav` al activar el limitador de pitlane.

C) RECORDATORIO DE TAREAS DE FÍSICA PENDIENTES (P1 / P2):
   - P1: Fidelidad del JSON `f1_94_physics.json` (superficies lateral/longitudinal grip assist, diff threshold).
   - P2: Telemetría CSV de la ruta Rust (`telemetry_manager.gd` soporte `F194RustVehicle` y columna `TC_Active`).

================================================================================
5. COMANDOS ÚTILES DE VERIFICACIÓN Y BUILD
================================================================================
- Compilar todo en Windows:
  `powershell -ExecutionPolicy Bypass -File .\scripts\build_windows.ps1 -Configuration debug`
- Ejecutar tests de Rust:
  `cd game/crates ; cargo test --workspace`
- Ejecutar test headless de Godot:
  `.tools\godot\Godot_v4.7.1-stable_win64_console.exe --headless --path game --quit-after 240 res://scenes/runtime/sim_bridge_quick_test.tscn`
