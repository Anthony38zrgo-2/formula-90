# Handoff — Mejoras de desacoplamiento y robustez de build (Formula90s)

Este documento es un handoff detallado del análisis y el plan de mejoras
derivado del incidente: "audio V10 silencioso + 7ª marcha no disponible"
en el coche f1_2026_2008. El objetivo es evitar que errores de este tipo
(DESINCRONIZACION de SHA/BUILD, constantes hardcodeadas, validacion
enganosa, acoplamiento entre capas) vuelvan a ocurrir con frecuencia.

================================================================================
0. CONTEXTO / INCIDENTE RAIZ
================================================================================

Sintomas:
  - El audio del V10 no sonaba en absoluto.
  - La 7ª marcha no estaba disponible (el coche solo subia hasta 6ª).

Causa raiz del AUDIO:
  - Desincronizacion de res://BUILD_SOURCE. El DLL compilado lleva embebido
    FORMULA90_BUILD_SHA = git rev-parse HEAD = 6d862847e5b8466267a25be907f168acfe94fce3,
    pero game/BUILD_SOURCE contenia d7d8a08fca460341cf1591f508bd58be4c7d1c14 (SHA viejo).
  - El gate de F90Core::load_dll (f90_core.cpp:221) fallaba con:
      FATAL: native BUILD does not match recorded source.
      Native=6d862847... recorded=d7d8a08f...
  - Eso hacia que el facade NUNCA se inicializara -> cero audio.
  - El coche se movia porque F194RustVehicle tiene fisica legacy propia,
    pero sin F90Core no hay cadena de audio.

Causa del BUG de 7ª:
  - Cap hardcodeado `current_gear < 6` en f1_94_rust_input_controller.gd.
  - YA CORREGIDO a data-driven leyendo gear_ratios.size() del JSON.

Fix YA APLICADO (parche temporal):
  - game/BUILD_SOURCE escrito con el HEAD actual.
  - build_windows.ps1 ahora regenera BUILD_SOURCE al inicio del build.
  - Esto desbloquea, pero NO PREVIENE que vuelva a desincronizarse.

================================================================================
1. HALLAZGOS CLAVE (EVIDENCIA)
================================================================================

1.1 Tres gates de BUILD/SHA en nativo:
  - F90Core            native/src/core/f90_core.cpp:212-224
                       (mismatch -> return false, early return) + 2a comprobacion :280-285
  - VehicleAudio...    native/src/presentation/vehicle_audio_controller_native.cpp:115-127
                       (mismatch -> return false) + :185-190
  - F194RustVehicle    native/src/vehicle/f1_94_rust_vehicle.cpp:286-288
                       SOLO IMPRIME el SHA; NO gatea.

1.2 SHA generado en 4 sitios independientes (puede derivar):
  - SConstruct:10-13                       -> C++ (libformula90s)
  - formula90-core/build.rs:4-15           -> crate Rust
  - vehicle-audio-engine/build.rs:4-15     -> crate Rust
  - vehicle-physics-engine/build.rs        -> hash CORTO, nunca comparado

1.3 -ValidateRuntimeOnly es un NO-OP que imprime "validado" sin verificar nada:
  - run_f1_94.ps1:210-213
  - run_f1_94_no_rebuild.ps1:187-190
  Enganoso: da sensacion de validacion exitosa cuando no la hay.

1.4 Ruta de config triplicada:
  - physics_config_path  (vehicle node, f1_94_rust_vehicle.hpp:72)
  - config_json_path     (F90Core,        f90_core.hpp:210)
  - F1_94_PHYSICS_CONFIG hardcodeado en telemetry_manager.gd:21
        BUG LATENTE: siempre lee el JSON de f1_94 aunque el coche sea otro.

1.5 Magic strings:
  - Bus "Vehicle" triplicado:
        ensure_vehicle_bus.gd:16,23
        f90_core.cpp:666,674,675
        vehicle_audio_controller_native.cpp:249,257,258
    Ademas el nodo de escena se llama "VehicleAudio" (nombre confuso).
  - Bank res://sounds/banks/v10_vehicle hardcodeado en
        vehicle_audio_controller_native.cpp:198
    (F90Core SI usa la propiedad bank_dir).

1.6 Error silencioso:
  - game-sim/src/c_abi.rs:117-120 devuelve 0 sin mensaje en fallo de parseo;
    formula90-core/src/ffi.rs SI escribe err_buf. Inconsistente.

1.7 Manifest physics_sha256 solo se chequea en un test Python
   (tools/vehicle_studio/tests/test_runtime_variant_2009.py:33);
   en runtime carga silenciosamente aunque no cuadre.

================================================================================
2. PLAN DE MEJORAS
================================================================================

--------------------------------------------------------------------------------
P0 — INTEGRIDAD DE BUILD (CRITICO: previene la recurrencia exacta)
--------------------------------------------------------------------------------

P0.1 Fuente unica de SHA via env-var  (ENFOQUE ELEGIDO)
  - scripts/build_windows.ps1:
      Calcular UNA vez:  $sha = (git rev-parse HEAD).Trim()
      Exportar:          $env:FORMULA90_BUILD_SHA = $sha
      ANTES de invocar SCons y cargo.
  - SConstruct:10-13:
      Leer FORMULA90_BUILD_SHA del entorno si existe; si no, git rev-parse HEAD.
  - formula90-core/build.rs:4-15  y  vehicle-audio-engine/build.rs:4-15:
      Leer std::env::var("FORMULA90_BUILD_SHA") primero; fallback a git.
  - (Opcional, consistencia) vehicle-physics-engine/build.rs:
      Alinear para usar el mismo SHA de 40 chars en f1_94_physics_build_sha().
  - RESULTADO: C++, los crates Rust y BUILD_SOURCE comparten un SHA identico
    e imposible de desincronizar. El gate runtime queda como backstop que
    nunca deberia dispararse.

P0.2 scripts/check_runtime_parity.ps1  (nuevo, pre-launch)
  - expected = git rev-parse HEAD
  - recorded = Get-Content game/BUILD_SOURCE (strip)
  - Assert expected == recorded  y  recorded.length() == 40.
    Con P0.1 esto garantiza que todos los DLL desplegados coinciden
    (porque se construyeron con ese mismo SHA).
  - (Opcional) Recalcular SHA256 del JSON de fisica de cada vehiculo y
    comparar con physics_sha256 de su manifest.json; fallar con mensaje
    accionable si difiere.
  - Llamar este script desde run_f1_94.ps1 y run_f1_94_no_rebuild.ps1
    ANTES de lanzar Godot.

P0.3 Arreglar -ValidateRuntimeOnly
  - Reemplazar el no-op (run_f1_94.ps1:210-213, run_f1_94_no_rebuild.ps1:187-190)
    por una llamada real a check_runtime_parity.ps1 (o al menos el assert
    HEAD==BUILD_SOURCE). Eliminar el mensaje enganoso "validado".

--------------------------------------------------------------------------------
P1 — DESACOPLAR GDSCRIPT DE LA CONFIG NATIVA
--------------------------------------------------------------------------------

P1.1 Exponer get_gear_count() / get_gear_ratios()
  - Anadir al FFI de F194RustVehicle (y/o F90Core) un getter que devuelva el
    nº de marchas del config cargado (el facade ya tiene el config).
  - El input controller usara vehicle.get_gear_count() en vez de re-leer/parsear
    el JSON: mantiene el beneficio data-driven pero elimina el duplicado de
    ruta/parseo. Conservar el fallback a JSON como red de seguridad.

P1.2 Autoridad unica de ruta de config
  - F90Core.config_json_path es la autoridad. En _ready del vehicle (o del
    F90Core), derivar physics_config_path del core cuando exista, eliminando
    la propiedad duplicada.
  - telemetry_manager.gd:21: reemplazar F1_94_PHYSICS_CONFIG hardcodeado por
    lectura de config_json_path del vehiculo/F90Core activo.

--------------------------------------------------------------------------------
P2 — ELIMINAR MAGIC STRINGS
--------------------------------------------------------------------------------

P2.1 Bank path
  - vehicle_audio_controller_native.cpp:198: sustituir el literal
    res://sounds/banks/v10_vehicle por la propiedad bank_dir compartida
    (igual que usa F90Core). Anadir propiedad bank_dir a
    VehicleAudioControllerNative que espeje la de F90Core.

P2.2 Nombre de bus constante
  - Definir const String VEHICLE_AUDIO_BUS = "Vehicle"; en un header
    compartido (o constante en .gd autoload) usado por ensure_vehicle_bus.gd,
    f90_core.cpp y vehicle_audio_controller_native.cpp.
  - Unificar el naming del nodo de escena ("VehicleAudio" -> algo coherente)
    para evitar confusion.

--------------------------------------------------------------------------------
P3 — SUPERFICIE DE ERRORES Y GUARDAS DE REGRESION
--------------------------------------------------------------------------------

P3.1 Unificar reporte de errores FFI
  - game-sim/src/c_abi.rs:117-120: en fallo de from_json_path, escribir
    err_buf + printerr en vez de return 0 silencioso.

P3.2 Log de skip de audio en headless
  - f90_core.cpp create_audio_nodes: cuando es headless,
    print("[F90Core] audio nodes skipped (headless)") para no parecer roto.

P3.3 Test de integracion cargo
  - Extender el test de vehicle-physics-engine para cargar cada JSON de
    vehiculo shipeado en game/data/vehicles/*/ y assert validate() pasa y
    gear_ratios.len() == traction_control_*.len().
    Detecta JSON obsoletos sin abrir Godot.

P3.4 Auto-regenerar physics_sha256 del manifest
  - En build_windows.ps1, tras copiar DLLs, recalcular el SHA256 del JSON de
    fisica y actualizar physics_sha256 en el manifest.json afectado para que
    no derive.

================================================================================
3. ORDEN DE IMPLEMENTACION SUGERIDO
================================================================================

  1. P0.1 -> P0.2 -> P0.3   (critico; previene la recurrencia exacta)
  2. P1.1 -> P1.2           (desacopla GDScript de la config nativa)
  3. P2.1 -> P2.2           (elimina magic strings)
  4. P3.1 -> P3.4           (robustez y regresion)

================================================================================
4. RIESGOS / NOTAS
================================================================================

- P0.1 toca SConstruct y los build.rs: conservar el fallback a
  `git rev-parse HEAD` para que builds fuera de CI (invocando scons/cargo
  directo) sigan funcionando.
- El gate runtime FATAL se mantiene como backstop; con P0.1 no deberia
  dispararse nunca.
- check_runtime_parity.ps1 es suficiente verificando HEAD == BUILD_SOURCE
  (gracias a P0.1 todos los DLL comparten ese SHA); no hace falta invocar
  exports de DLL desde PowerShell.
- Verificar que el cambio de env-var no rompa bootstrap_windows.ps1 ni
  pipelines que llamen a scons/cargo directamente (usaran el fallback git).
- Mantener el principio RUNTIME PARITY del AGENTS.md: el runtime debe
  rechazar binarios cuyo BUILD no coincida con HEAD; P0 lo hace fallar
  rapido y ANTES del launch, no como FATAL silencioso dentro de Godot.

================================================================================
HANDOFF — WHEELSPIN PERSISTENTE EN f1_2026_2008 + AUDIO SILENCIOSO
(2026-08-29, rama f1-94)
================================================================================

Sintomas reportados:
  - Wheelspin fuerte en practicamente todas las fases; las traseras se
    quemaban (tread 114 C, pico) aun con TC activo (corta 0.65-0.78).
  - Despues de los primeros fixes: la punta mejora mucho (slip 0.08-0.13
    @240 km/h) pero 1a-2a y las salidas de 3a-4a-5a siguen girando
    (RL_TCSlip 0.2-0.47, TC al limite de su autoridad en marcha alta).
  - Audio V10 silencioso (el banco y mixer cargaban; no sonaba).

CAUSA RAIZ del audio (resuelta):
  - f90_core.cpp create_audio_nodes() gateaba audio_initialized_ en
    get_stream_playback() llamado en el MISMO frame que play(); Godot
    devuelve null hasta un frame despues -> pump audio muerto para
    siempre (audio_initialized=false, [F90Core] facade ready).
  - (Recordatorio: la escena 2026 es FACADE-driven. F90Core conduce
    fuerzas y audio via formula90_core.dll; el solver legacy
    f1_94_physics_solve_forces solo lo usa F194RustVehicle suelto.)

CAUSAS del wheelspin persistente (evidencia telemetria 120 Hz,
telemetry_20260829_013507_765.csv y sesiones 0204-0215):
  1. SUELO/DIFUSOR MUERTO en la ventana 100-170 km/h: UF_Center_Clearance
     ~0.098-0.100 m vs underfloor.optimal_height_m 0.040 (!!), rake real
     0.15 deg, expansion -0.14 deg vs objetivo +3.0 -> DiffuserStallFactor
     0.17 -> FloorDownforce solo ~150 N. El agarre lo aporta el ala trasera
     (~850 N @145) unicamente.
  2. El cambio optimal_height 0.045 -> 0.040 FUE CONTRAproducente (sube la
     ineficiencia: el coche nunca llega a ese optimo).
  3. DIFERENCIAL ASIMETRICO: en RECTA (steer=0, LatG=0) RR recibe 1225 Nm
     vs RL 747 (preload 60 + rampas 68/78). Techo de reaccion RR ~1092 Nm
     (mu efectivo = 2.60 x 0.64 x load_scale(~0.98, exp 0.92) = ~1.63xFz)
     -> RR arranca el spin y este se sostiene.
  4. TC sin techo en marchas altas: max_cut 4a-6a = 0.30/0.14/0.07 y
     authority 0.45/0.20/0.10 -> una vez pasado el pico de mu no lo
     recupera (slip 0.445 con cut clavado en 0.14 en 5a).

CAMBIOS YA APLICADOS (commits en rama f1-94):
  - 2cca38c2 feat(physics): limitador de ataque de par
    (torque_attack_rate_nm_s, def. 0 = legacy; rampa +rate*dt, recorte
    instantaneo); techo inicial 514->480, final_drive 5.15->4.4,
    max_clutch_torque_ratio 1.10->0.85; aero nominal legacy en
    f1_94_physics_solve_forces ((AeroEnvironment del aero_model));
    fallback aero de fachada en step_with_underfloor cuando valid_mask==0;
    tests (engine_torque_attack_tests, facade_aero_fallback) + DLLs
    vehicle_physics_engine/game_sim/formula90_core + manifest sha.
  - 109c08f0 fix(audio): get_stream_playback() diferido (con retry en el
    pump) en F90Core y VehicleAudioControllerNative; libformula90s.dll.
  - 557d3640 tune(f1-2026-2008): max_torque 480->440, final_drive
    4.4->4.15, attack 1800->1100, throttle smoothing 7.5->4.0,
    rear_wing area 0.84->0.90, underfloor optimal_height 0.045->0.040,
    diff preload 40->60; manifest sha.
  - SIN COMMIT (arbol): final_drive 4.15->4.05 (edicion de usuario),
    manifest sha a 4.05 (A1B6C3...), run_f1_94.ps1: los cheques de hash
    de perfil fisico (2026/2009) pasaron de throw a Write-Warning (ya no
    bloquea la edicion del JSON; los hashes de assets y GLB de circuito
    SI siguen bloqueando).

CLAVE DE ARQUITECTURA (no olvidar):
  - El JSON de fisica se carga EN RUNTIME por la fachada
    (config_json_path = res://data/vehicles/f1_2026_2008/...json).
    Cambiar el JSON NO requiere recompilar DLLs; si requiere refrescar
    physics_sha256 en manifest.json (ahora: solo avisa, no bloquea).
  - Los DLL si se recompilan solo cuando cambia Rust/FFI/C++
    (scripts/build_windows.ps1 para C+++crates + SCons, o cargo build + copy
    manual; game_sim/formula90_core INCRUSTAN vehicle_physics_engine como
    rlib -> reconstruirlos si cambia la fisica para la escena 2026).

PLAN PENDIENTE (recalibracion data-driven, pendiente de aplicar):
  1. underfloor.optimal_height_m 0.040 -> ~0.075 (coincidir con el
     clearance real de operacion; devuelve factor 0.58->~0.79 en la
     ventana y sigue optimo en punta: 0.0696 @240).
  2. underfloor.optimal_expansion_deg 3.0 -> ~1.0 y expansion_window_deg
     5.0 -> ~8.0 (desectar el difusor; expansion real -0.14).
  3. underfloor.optimal_rake_deg 0.5 -> ~0.25 y rake_window_deg 2.5 ->
     ~3.5 (rake real 0.15).
  4. differential.preload_nm 60 -> ~45 y power_ramp_angle_deg 68 -> ~60
     (menos transferencia asimetrica -> menos spin de rueda individual).
  5. surfaces.Road.longitudinal_grip_ratio 0.64 -> ~0.73 (+~14% techo de
     traccion, la palanca "sin TC" directa).
  6. aids.traction_control_gear_max_cut 4a-6a 0.30/0.14/0.07 -> 0.45/0.30/
     0.18 y gear_authority 0.45/0.20/0.10 -> 0.60/0.35/0.20 (red de
     seguridad en marchas altas, no dependencia).
  7. tires.rear.contact_patch 0.36 -> ~0.38 (mu trasero sutil).

NOTAS / RIESGOS:
  - No depender del TC: la palanca real es aero funcional + mu (items
    1-5). El 6 es solo para no quedar colgado tras un pico de spin.
  - Verificar cada cambio en pista con run_f1_94.ps1 y reanalizar
    game/telemetry/telemetry_*.csv (columnas Aero_*, UF_*,
    RL/RR_DriveTorquePreTC_Nm, TC_CutRatio, RL/RR_TCSlip).
  - Si se sube mucho el ala trasera / mu, vigilar temperaturas traseras
    (optico 83-107 C; sobre 120) y el balance (Aero_BalanceFront ~0.34).
  - El launcher ya no bloquea por hash de fisica (solo avisa); si se
    quiere la salvaguarda estricta de nuevo, revertir run_f1_94.ps1.
