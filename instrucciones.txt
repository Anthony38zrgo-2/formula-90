================================================================================
FORMULA-90: INFORME DE HANDOFF Y PLAN DE ALINEACIÓN DE F1_94_PHYSICS.JSON
================================================================================
Fecha: 2026-08-17
Rama/Workspace: D:\Formula90s
Script Canónico de Ejecución: scripts/run_f1-94.ps1
Destino de Deprecación: D:\f1-jordan-197-deprecated

--------------------------------------------------------------------------------
1. RESUMEN DEL ESTADO ACTUAL DEL PROYECTO
--------------------------------------------------------------------------------
- Limpieza y deprecación previa: Se movieron 85 elementos obsoletos (modelos GLB,
  escenas, scripts y datos de Jordan 191/197/1995, F1 2026 y baseline 2026) a
  D:\f1-jordan-197-deprecated. La carpeta `game/` quedó 100% enfocada en F1-94 y
  la pista La Chutana.
- Análisis de 5 factores de discrepancia en la cadena canónica completado.
- Código implementado al 100% en las 3 capas:
  1. Rust Physics Engine (`game/physics/engine/`)
  2. C++ GDExtension (`native/`)
  3. GDScript Gameplay & Controllers (`game/addons/formula90s/scripts/`)

--------------------------------------------------------------------------------
2. DETALLE DE LOS 5 PUNTOS RESUELTOS EN EL CÓDIGO
--------------------------------------------------------------------------------

[PUNTO 1] Fallback silencioso en Rust / C++ (deny_unknown_fields):
  - Archivos:
    * `game/physics/engine/src/vehicle_config.rs`
    * `game/physics/engine/src/ffi.rs`
    * `native/src/vehicle/f1_94_rust_vehicle.cpp`
  - Solución aplicada:
    * Se agregó `#[serde(default, deny_unknown_fields)]` a TODAS las subestructuras
      del JSON (`JsonChassis`, `JsonGeometry`, `JsonSteering`, `JsonPowertrain`,
      `JsonDifferential`, `JsonSuspension`, `JsonSuspensionAxle`, `JsonTires`,
      `JsonTireAxle`, `JsonSurfaceEntry`, `JsonBrakes`, `JsonAero`, `JsonAeroSplit`,
      `JsonVec3`, `JsonAutomaticShift`).
    * Se mapearon `automatic_shift` y `wheel_hubs` en las estructuras de Serde.
    * Si el JSON falla la deserialización, C++ emite un error explícito y visible
      vía `printerr("[F194RustVehicle] CRITICAL JSON PARSE ERROR: ...")`.

[PUNTO 2] Ayudas de conducción sobrescribiendo parámetros (DrivingAidsController):
  - Archivo: `game/addons/formula90s/scripts/driving_aids.gd`
  - Solución aplicada:
    * `_capture_baseline()` ahora lee el valor de `automatic_transmission` directamente
      del vehículo/JSON y establece `aids[0] = bool(auto_trans)` dinámicamente.
    * `aids` se inicializa con `[false, false, false, false, false]`, evitando
      sobrescribir `automatic_transmission: false` en `_ready()`.
    * Al restaurar la ayuda 0, se recupera el baseline original del JSON.

[PUNTO 3] Inercia en Godot RigidBody vs Rust:
  - Archivos:
    * `game/physics/engine/src/ffi.rs` (ABI v7, campos `inertia_multiplier_x/y/z`)
    * `native/include/formula90s/vehicle/formula90_physics.h`
    * `native/include/formula90s/vehicle/f1_94_rust_vehicle.hpp`
    * `native/src/vehicle/f1_94_rust_vehicle.cpp`
  - Solución aplicada:
    * FFI transfiere `inertia_multiplier_x`, `inertia_multiplier_y`, `inertia_multiplier_z`
      desde el JSON / Rust hacia C++.
    * En `solve_forces_for_state()`, se calcula `configured_inertia` multiplicando
      `base_inertia` por los multiplicadores individuales del JSON (`1.05, 1.15, 1.05`),
      reemplazando el factor fijo `1.10f`.

[PUNTO 4] Desplazamiento visual de suspensión vs Fuerzas físicas:
  - Archivos:
    * `game/physics/engine/src/ffi.rs`
    * `native/include/formula90s/vehicle/formula90_physics.h`
    * `native/include/formula90s/vehicle/f1_94_rust_vehicle.hpp`
    * `native/src/vehicle/f1_94_rust_vehicle.cpp`
  - Solución aplicada:
    * Se expusieron a través de FFI `suspension_front_spring_length`, `suspension_rear_spring_length`,
      `suspension_front_resting_ratio` y `suspension_rear_resting_ratio`.
    * En `update_wheel_visuals()`, se reemplazaron las constantes fijas (0.250/0.180 y 0.400/0.350)
      por las variables sincronizadas desde el JSON/Rust.
    * El ángulo de dirección visual de fallback usa `max_steering_angle_` del JSON.

[PUNTO 5] Modulación de entrada en F194RustInputController:
  - Archivo: `game/addons/formula90s/scripts/f1_94_rust_input_controller.gd`
  - Solución aplicada:
    * Se agregó `@export var throttle_exponent: float = 1.0`.
    * La lectura del acelerador ahora es lineal 1:1 por defecto, eliminando la
      zona muerta cuadrática (`pow(..., 2.0)`) y entregando la curva de par exacta
      del JSON al motor.

--------------------------------------------------------------------------------
3. ARCHIVOS MODIFICADOS
--------------------------------------------------------------------------------
1. `game/physics/engine/src/vehicle_config.rs`
   - `deny_unknown_fields` en todos los structs JSON.
   - Declarados `JsonAutomaticShift` y `wheel_hubs`.
   - Tests de regresión para typos en subestructuras (`json_tests`).

2. `game/physics/engine/src/ffi.rs`
   - ABI bump a v7 (`F1_94_PHYSICS_ABI_VERSION = 7`).
   - `FfiRuntimeConfig` ampliado con inercia y suspensión.
   - Sincronización en `get_runtime_config` y `apply_runtime_config`.

3. `native/include/formula90s/vehicle/formula90_physics.h`
   - ABI bump a v7.
   - `F90RuntimeConfig` actualizado con los nuevos campos de inercia y suspensión.

4. `native/include/formula90s/vehicle/f1_94_rust_vehicle.hpp`
   - Nuevos miembros: `inertia_multiplier_x/y/z_`, `suspension_front/rear_spring_length_`,
     `suspension_front/rear_resting_ratio_`.

5. `native/src/vehicle/f1_94_rust_vehicle.cpp`
   - Log de error explícito en `_ready()`.
   - Inercia dinámica en `solve_forces_for_state()`.
   - Posicionamiento visual dinámico en `update_wheel_visuals()`.
   - Sincronización en `sync_runtime_config_from_rust()` y `apply_runtime_config()`.

6. `game/addons/formula90s/scripts/driving_aids.gd`
   - Inicialización dinámica de `aids[0]` desde el vehículo.
   - Restauración respetando baseline.

7. `game/addons/formula90s/scripts/f1_94_rust_input_controller.gd`
   - Exponente de aceleración lineal configurable (`throttle_exponent = 1.0`).

--------------------------------------------------------------------------------
4. COMANDOS PARA RETOMAR Y VALIDAR EL SISTEMA
--------------------------------------------------------------------------------
Para el siguiente agente o desarrollador, ejecutar los siguientes pasos en orden:

Paso 1: Compilar la DLL de C++ GDExtension (Release y Debug):
  scons platform=windows target=template_release
  scons platform=windows target=template_debug

Paso 2: Validar tests unitarios de Rust:
  cargo test --manifest-path game/physics/engine/Cargo.toml

Paso 3: Validar físicas de F1-94 en Godot:
  powershell -ExecutionPolicy Bypass -File scripts/run_f1-94.ps1 -TestPhysics

Paso 4: Ejecutar Smoke Tests completos (HUD + Minimapa + Mountains3D + Audio):
  powershell -ExecutionPolicy Bypass -File scripts/run_f1-94.ps1 -Smoke
  powershell -ExecutionPolicy Bypass -File scripts/run_f1-94.ps1 -SmokeAudio

Paso 5: Ejecutar Suite Automatizada Completa:
  powershell -ExecutionPolicy Bypass -File scripts/test_windows.ps1

Paso 6: Ejecutar el juego en vivo:
  powershell -ExecutionPolicy Bypass -File scripts/run_f1-94.ps1
================================================================================
