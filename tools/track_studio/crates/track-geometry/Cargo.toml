[package]
name = "track-geometry"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
track-domain = { path = "../track-domain" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
