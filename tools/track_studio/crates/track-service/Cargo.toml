[package]
name = "track-service"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
track-domain = { path = "../track-domain" }
track-storage = { path = "../track-storage" }
track-commands = { path = "../track-commands" }
track-geometry = { path = "../track-geometry" }
track-validation = { path = "../track-validation" }
track-assets = { path = "../track-assets" }
track-export = { path = "../track-export" }
track-build = { path = "../track-build" }
