[package]
name = "track-export"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
track-domain = { path = "../track-domain" }
track-geometry = { path = "../track-geometry" }
