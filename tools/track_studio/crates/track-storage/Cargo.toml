[package]
name = "track-storage"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rusqlite = { version = "0.40.1", features = ["bundled"] }
serde_json = "1"
track-domain = { path = "../track-domain" }
