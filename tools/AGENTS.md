# Herramientas

Los pipelines Python de sprites y audio se implementarán en Fase 2. Python nunca pertenece al runtime. No añadir dependencias antes de diseñar el pipeline ni escribir directamente en directorios finales: generar en staging, validar y luego promover.

